{% if posts|length > 0 %}
  <div class="searchList">
    {% for post in posts  %}
      <a class="searchList-item" href="{{ post.link }}">
        <article class="post">
          <div class="post-title h3">{{ post.title }}</div>
        </article>
      </a>
    {% endfor %}
  </div>
{% else %}
  <div class="noResults">
    <p>{{ labels.noResults }}</p>
  </div>
{% endif %}
