<div class="flyntComponent" is="flynt-block-digital-hero-image">
  <div class="container ">
    <figure class="figure">
      <img class="content-image lazyload"
        src="{{ image.src|resize(32) }}"
        data-srcset="{{function('wp_get_attachment_image_srcset', image.id)}}"
        data-sizes="auto">
    </figure>
  </div>
</div>
